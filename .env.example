# AI Navigator 环境变量配置模板
# 复制此文件为 .env 并填入真实的 API 密钥

# =============================================================================
# AI Provider 配置
# =============================================================================

# AI 提供商选择: 'anthropic' 或 'openai'
AI_PROVIDER=anthropic

# --- Anthropic Claude 配置 (当 AI_PROVIDER=anthropic 时使用) ---
# 获取 API Key: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# --- OpenAI 兼容 API 配置 (当 AI_PROVIDER=openai 时使用) ---
# 支持: 七牛 AI Token API, OpenAI 官方, Azure OpenAI 等
# OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_BASE_URL=https://api.qiniu.com/v1
# OPENAI_MODEL=gpt-3.5-turbo

# =============================================================================
# 高德地图 MCP Server 配置
# =============================================================================

# 方式一: 使用远程 MCP 服务器 URL (推荐)
AMAP_MCP_SERVER_URL=https://mcp.amap.com/sse

# 方式二: 使用本地 MCP 服务器路径
# AMAP_MCP_SERVER_PATH=/path/to/amap-mcp-server

# 高德地图 API Key (可选, 不设置将使用 Mock 模式)
# 获取 API Key: https://lbs.amap.com/
AMAP_API_KEY=your-amap-api-key-here

# =============================================================================
# 使用说明
# =============================================================================
# 1. 将此文件复制为 .env:
#    cp .env.example .env
#
# 2. 编辑 .env 文件，填入真实的 API 密钥
#
# 3. .env 文件已被 .gitignore 排除，不会提交到版本控制
#
# 4. 环境变量优先级: 系统环境变量 > .env 文件
#
# 5. 生产环境建议使用系统环境变量或密钥管理服务
